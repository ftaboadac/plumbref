# Plumbref Benchmark Scaffold

This directory is reserved for raw outputs from the claim-check benchmark.

Do not publish benchmark claims until this contains:

- baseline prompt
- Plumbref prompt
- per-task outputs
- human review notes
- final benchmark report

Use the plan in `docs/claim-check-benchmark-plan.md`.
